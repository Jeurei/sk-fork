export type Car = {
    car_id: number;
    // code: string;
    color: string;
    gos_number: string;
    // is_locked: boolean;
    mark: string;
    model: string;
    // name: string;
    // order_params: number[];
    // production_year: number;
    // short_name: string;
}
